bin/
obj/
.vs/
.vscode/
.idea/
TestResults/
*.user
*.suo
*.userosscache
*.sln.docstates
*.nupkg
*.snupkg
artifacts/
.env
.env.*
!.env.example
appsettings.Local.json
secrets.json

# Contains real AiCompliance (FPT.AI/Gemini/Groq) keys directly -- moved out of
# dotnet user-secrets by request. Never remove this line without moving the
# real keys back out of the file first.
src/StreetBiz.API/appsettings.Development.json

# Uploaded evidence files (personal data) in local development
App_Data/

# API console log captured for OTP codes (scripts/e2e-auth-onboarding.sh)
api.log
